import * as PIXI from 'pixi.js';

/** Minimal loader wrapper. Replace with your own manifest/atlas setup. */
export async function loadBattleAssets(): Promise<void> {
  // TODO: Add calls like:
  // await PIXI.Assets.load({ src: 'path/to/atlas.json', alias: 'battle_atlas' });
  // PIXI.Texture.from('barrel_basic') should resolve afterwards.
}
